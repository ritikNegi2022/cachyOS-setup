[Login]
HandlePowerKey=ignore
HandlePowerKeyLongPress=ignore
# Let acpid handle power button -> lock screen via /etc/acpi/power-btn.sh
# (calls screen-lock = slock black; apps keep running, unlock with login password).
# Systemd-logind would otherwise do poweroff on short press
# HandleLidSwitch etc. keep defaults (suspend)
